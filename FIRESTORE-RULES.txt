rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Products: public storefront can read; only signed-in admin should write in production.
    match /products/{document=**} {
      allow read: if true;
      allow write: if request.auth != null && request.auth.token.email == "shoaibahmad52535455@gmail.com";
    }

    // Orders: checkout creates orders; admin manages them.
    // NOTE: For a production store, use Firebase Auth for customers or a Cloud Function.
    match /orders/{orderId} {
      allow create: if true;
      allow get: if true;
      allow list, update, delete: if request.auth != null && request.auth.token.email == "shoaibahmad52535455@gmail.com";
    }
  }
}
